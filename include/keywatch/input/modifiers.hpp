#pragma once

namespace keywatch::input {

// True while a Shift key is physically held down.
bool shift_down();

// True when Caps Lock is toggled on.
bool capslock_on();

} // namespace keywatch::input
